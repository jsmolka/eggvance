#include "attribute1.h"

Attribute1::Attribute1(u16 data)
    : data(data)
    , x(this->data)
    , paramter(this->data)
    , hor_flip(this->data)
    , ver_flip(this->data)
    , obj_size(this->data)
{

}
