#include "attribute0.h"

Attribute0::Attribute0(u16 data)
    : data(data)
    , y(this->data)
    , rotation(this->data)
    , double_size(this->data)
    , hide(this->data)
    , gfx_mode(this->data)
    , mosaic(this->data)
    , color_mode(this->data)
    , obj_shape(this->data)
{

}
